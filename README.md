# Dnhyxc AI - 项目全面介绍

## 📋 项目概述

Dnhyxc AI 是一个基于 **Tauri 2.x + React 19.x + NestJS 11.x** 构建的跨平台 AI 桌面应用程序。项目采用 Monorepo 架构，使用 pnpm 作为包管理器，支持 macOS 桌面应用开发和完整的后端服务部署。

## 🏗️ 项目架构

### 整体目录结构

```
dnhyxc-ai/
├── apps/                    # 应用层
│   ├── frontend/           # Tauri + React 桌面应用
│   └── backend/            # NestJS 后端服务
├── packages/               # 共享工具包
│   └── scripts/            # 构建和发布脚本
├── pnpm-workspace.yaml     # pnpm 工作区配置
├── biome.json              # 代码格式化和检查配置
├── commitlint.config.js    # Git 提交规范配置
└── docker-compose*.yml     # Docker 容器编排配置
```

### 技术架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      Dnhyxc AI                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌─────────────────────┐      ┌─────────────────────┐     │
│   │   Frontend (Tauri)  │      │    Backend (NestJS) │     │
│   │   ┌─────────────┐   │      │   ┌─────────────┐   │     │
│   │   │   React 19  │   │      │   │   NestJS 11 │   │     │
│   │   │   MobX      │   │◄────►│   │   TypeORM   │   │     │
│   │   │   Vite      │   │ HTTP │   │   MySQL     │   │     │
│   │   │   Radix UI  │   │      │   │   Redis     │   │     │
│   │   └─────────────┘   │      │   └─────────────┘   │     │
│   └─────────────────────┘      └─────────────────────┘     │
│                                                             │
│   ┌─────────────────────────────────────────────────────┐  │
│   │              Tauri Native Features                  │  │
│   │  • System Tray        • Global Shortcuts           │  │
│   │  • File System        • Clipboard Manager          │  │
│   │  • Auto-start         • Updater (Auto-update)      │  │
│   └─────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🚀 核心功能模块

### 前端功能 (apps/frontend)

#### 1. 用户界面

- **框架**: React 19.x + TypeScript 5.x
- **构建工具**: Vite 7.x
- **样式方案**: Tailwind CSS 4.x + Radix UI
- **状态管理**: MobX 6.x
- **路由**: React Router 7.x
- **表单处理**: React Hook Form + Zod + @hookform/resolvers

#### 2. Tauri 原生集成

- **系统托盘**: 最小化到托盘运行
- **全局快捷键**: 自定义快捷键支持
- **文件操作**: 本地文件读写和管理
- **剪贴板**: 复制粘贴功能
- **自动更新**: GitHub Release 自动更新检测和下载
- **开机自启**: 系统启动时自动运行

#### 3. 核心工具库

| 依赖                     | 版本     | 用途           |
| ------------------------ | -------- | -------------- |
| axios                    | ^1.13.2  | HTTP 请求      |
| crypto-js                | ^4.2.0   | 数据加密       |
| js-md5                   | ^0.8.3   | MD5 哈希       |
| qiniu-js                 | ^3.4.3   | 七牛云对象存储 |
| lucide-react             | ^0.561.0 | 图标库         |
| sonner                   | ^2.0.7   | 消息提示       |
| next-themes              | ^0.4.6   | 主题切换       |
| class-variance-authority | ^0.7.1   | 组件变体       |

#### 4. 前端项目结构

```
apps/frontend/
├── src/
│   ├── components/      # 公共组件
│   │   └── ui/         # UI 基础组件 (Radix UI + Tailwind)
│   ├── views/          # 页面视图
│   │   └── setting/    # 设置页面
│   ├── stores/         # MobX 状态管理
│   ├── utils/          # 工具函数
│   │   ├── updater.ts  # 更新检测
│   │   ├── cache.ts    # 缓存管理
│   │   ├── fetch.ts    # HTTP 客户端
│   │   └── ...
│   ├── hooks/          # React Hooks
│   ├── lib/            # 库配置
│   ├── assets/         # 静态资源
│   └── main.tsx        # 入口文件
├── src-tauri/          # Tauri 后端
│   ├── src/            # Rust 命令
│   │   ├── command/    # 命令实现
│   │   ├── system/     # 系统功能 (托盘、快捷键等)
│   │   └── utils/      # 工具函数
│   ├── Cargo.toml      # Rust 依赖配置
│   ├── tauri.conf.json # Tauri 配置文件
│   └── build.rs        # 构建脚本
└── index.html          # HTML 入口
```

### 后端功能 (apps/backend)

#### 1. 技术栈

| 技术     | 版本     | 用途               |
| -------- | -------- | ------------------ |
| NestJS   | ^11.1.11 | Node.js 服务端框架 |
| TypeORM  | ^0.3.28  | 数据库 ORM         |
| MySQL    | 8.0+     | 关系型数据库       |
| Redis    | 6.0+     | 缓存和会话         |
| JWT      | ^11.0.2  | 身份认证           |
| Passport | ^0.7.0   | 认证中间件         |
| CASL     | ^6.7.5   | 权限控制           |
| Winston  | ^3.19.0  | 日志管理           |
| Swagger  | ^11.2.3  | API 文档           |

#### 2. 核心模块

- **认证授权**: JWT + Passport + CASL 权限体系
- **用户管理**: 用户注册、登录、信息管理
- **文件管理**: 文件上传、下载管理 (本地 + 七牛云)
- **缓存管理**: Redis 缓存 + Keyv
- **邮件服务**: Nodemailer 邮件发送
- **安全防护**: Helmet + Rate Limit + 密码加密 (bcrypt/Argon2)

#### 3. 后端项目结构

```
apps/backend/
├── src/
│   ├── migrations/      # 数据库迁移文件
│   ├── modules/         # 功能模块
│   │   ├── auth/       # 认证模块
│   │   ├── users/      # 用户模块
│   │   ├── files/      # 文件模块
│   │   └── ...
│   ├── common/         # 公共模块
│   │   ├── decorators/ # 自定义装饰器
│   │   ├── filters/    # 异常过滤器
│   │   ├── guards/     # 守卫
│   │   ├── interceptors/ # 拦截器
│   │   └── pipes/      # 管道
│   ├── config/         # 配置模块
│   ├── main.ts         # 应用入口
│   └── app.module.ts   # 根模块
├── ormconfig.ts        # TypeORM 配置
├── test/               # 测试文件
└── jest.config.js      # Jest 测试配置
```

### 构建脚本 (packages/scripts)

| 脚本              | 文件                 | 功能                |
| ----------------- | -------------------- | ------------------- |
| bump-version      | bump-version.ts      | 版本号自增          |
| update-latest     | update-latest.ts     | 更新 latest.json    |
| upload-to-release | upload-to-release.ts | 上传 GitHub Release |

## 🛠️ 开发环境

### 环境要求

| 依赖    | 要求版本  | 说明              |
| ------- | --------- | ----------------- |
| Node.js | >= 18     | JavaScript 运行时 |
| Rust    | >= 1.70   | Tauri 编译依赖    |
| MySQL   | >= 8.0    | 主数据库          |
| Redis   | >= 6.0    | 缓存数据库        |
| pnpm    | >= 10.8.1 | 包管理器          |
| Docker  | latest    | 容器化部署 (可选) |

### 开发命令

```bash
# 启动前端开发服务器 (Tauri + Vite)
pnpm dev

# 启动后端开发服务器 (NestJS 热重载)
pnpm server:dev

# 构建生产版本
pnpm build

# 代码检查和格式化
pnpm check        # Biome 检查并修复
pnpm format       # Biome 格式化

# Git 提交
pnpm commit       # 规范化提交信息
```

### 后端专用命令

```bash
# 数据库迁移
pnpm m:g <name>   # 生成迁移文件
pnpm m:run        # 执行迁移
pnpm m:revert     # 回滚迁移

# 测试
pnpm test         # 单元测试
pnpm test:cov     # 测试覆盖率
pnpm test:e2e     # 端到端测试
```

## 📦 依赖管理

### 前端依赖 (apps/frontend/package.json)

**核心依赖**:

- `@tauri-apps/api` ^2 - Tauri API
- `@tauri-apps/plugin-*` - 各类 Tauri 插件
- `react` ^19.1.0 + `react-dom` ^19.1.0
- `mobx` ^6.15.0 + `mobx-react` ^9.2.1
- `react-router` ^7.10.1
- `axios` ^1.13.2

**UI 组件**:

- `@radix-ui/react-*` - 无样式可访问组件
- `tailwindcss` ^4.1.18
- `lucide-react` ^0.561.0
- `sonner` ^2.0.7

### 后端依赖 (apps/backend/package.json)

**核心依赖**:

- `@nestjs/*` ^11.x - NestJS 核心模块
- `@nestjs/typeorm` ^10.0.2 - TypeORM 集成
- `typeorm` ^0.3.28 - ORM 框架
- `mysql2` ^3.16.0 - MySQL 驱动
- `@nestjs/jwt` ^11.0.2 - JWT 模块
- `@nestjs/passport` ^11.0.5 - Passport 集成
- `@nestjs/cache-manager` ^3.1.0 - 缓存模块

**工具依赖**:

- `argon2` ^0.44.0 - 密码哈希
- `bcrypt` ^6.0.0 - 密码加密
- `winston` ^3.19.0 - 日志管理
- `nodemailer` ^7.0.12 - 邮件发送
- `multer` ^2.0.2 - 文件上传
- `qiniu` ^7.14.0 - 七牛云 SDK

### 开发工具

| 工具       | 版本    | 用途             |
| ---------- | ------- | ---------------- |
| Biome      | ^2.3.9  | 代码检查和格式化 |
| Husky      | ^8.0.3  | Git Hooks 管理   |
| Commitizen | -       | 规范化提交信息   |
| ESLint     | ^9.18.0 | 代码质量检查     |
| Prettier   | ^3.4.2  | 代码格式化       |
| Jest       | ^30.0.0 | 测试框架         |

## 🔐 安全特性

### 前端安全

- Tauri 沙箱隔离
- CSP 内容安全策略
- 敏感信息本地存储

### 后端安全

- **身份认证**: JWT Token + Refresh Token
- **密码加密**: Argon2 + bcrypt 双保险
- **API 防护**: Helmet 安全头 + Rate Limit 限流
- **权限控制**: CASL 基于角色的访问控制
- **输入验证**: Joi + class-validator 双重验证
- **SQL 防护**: TypeORM 参数化查询

## 📱 桌面应用特性

### 系统集成

- **系统托盘**: 自定义托盘菜单、最小化到托盘
- **全局快捷键**: 自定义快捷键注册和管理
- **文件访问**: 本地文件系统读写
- **剪贴板**: 文本和富文本复制粘贴
- **开机自启**: 系统启动时自动运行
- **自动更新**: GitHub Release 版本检测和更新

### 更新机制

```typescript
// 检测更新流程
1. 前端调用 checkVersion()
2. 后端清除 updater 缓存
3. 拉取 GitHub latest.json
4. 对比版本号
5. 下载新版本并安装
```

## 🐳 Docker 部署

### 开发环境

```bash
docker-compose up -d
```

### 生产环境

```bash
docker-compose -f docker-compose-prod.yml up -d
```

### 服务配置

- 前端: Tauri 桌面应用 (本地运行)
- 后端: NestJS API 服务
- MySQL: 主数据库
- Redis: 缓存服务

## 📊 项目状态

### ✅ 已完成功能

- [x] Monorepo 项目架构搭建
- [x] Tauri 2.x 桌面应用框架
- [x] React 19.x 前端界面
- [x] NestJS 11.x 后端服务
- [x] 用户认证系统 (JWT)
- [x] 文件上传管理
- [x] 系统托盘集成
- [x] 全局快捷键支持
- [x] GitHub Release 自动更新
- [x] Docker 容器化部署
- [x] 代码规范 (Biome + Husky)

### 🚧 开发中功能

- [ ] AI 功能集成
- [ ] 提示词模板管理
- [ ] 更多 AI 模型支持

## 🔧 配置说明

### 环境变量

#### 后端 (.env)

```env
# 应用配置
NODE_ENV=development
PORT=9112

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=your_password
DB_DATABASE=dnhyxc_ai_db

# JWT 配置
JWT_SECRET=your_jwt_secret
JWT_EXPIRES_IN=7d

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379

# 邮件配置
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USER=your_email
MAIL_PASSWORD=your_password

# 七牛云配置
QINIU_ACCESS_KEY=your_access_key
QINIU_SECRET_KEY=your_secret_key
QINIU_BUCKET=your_bucket
QINIU_DOMAIN=your_domain
```

#### 前端 (Tauri)

前端配置主要通过 `apps/frontend/src-tauri/tauri.conf.json` 管理：

- 应用窗口配置
- 系统托盘配置
- 权限配置
- 更新检测 URL

## 📝 代码规范

### 提交规范

项目使用 **Conventional Commits** 规范：

```
<type>[optional scope]: <description>

[optional body]

[optional footer(s)]
```

**Type 类型**:

- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试
- `chore`: 构建/工具

### 代码检查

```bash
# 检查并修复
pnpm check

# 仅检查
pnpm check --no-apply
```

## 🤝 贡献指南

1. Fork 本项目
2. 创建功能分支: `git checkout -b feature/AmazingFeature`
3. 提交更改: `pnpm commit`
4. 推送到分支: `git push origin feature/AmazingFeature`
5. 开启 Pull Request

## 📄 许可证

本项目采用 ISC 许可证 - 详见 [LICENSE](LICENSE) 文件。

## 👨‍💻 作者

**dnhyxc** - [GitHub](https://github.com/dnhyxc) - dnhyxc@gmail.com

## 🙏 致谢

- [Tauri](https://tauri.app/) - 跨平台桌面应用框架
- [React](https://reactjs.org/) - 用户界面库
- [NestJS](https://nestjs.com/) - Node.js 服务端框架
- [TypeScript](https://www.typescriptlang.org/) - 类型安全的 JavaScript
- [Radix UI](https://www.radix-ui.com/) - 可访问的 UI 组件
- [Tailwind CSS](https://tailwindcss.com/) - 实用优先的 CSS 框架
